Script to setup Eslint & Prettier in Typescript projects.

Run `npx lintier` in your project's directory to start the interactive prompt.

Or use the available flags:

* -r React
* -n Node
* -a Airbnb
* -s Stylelint
* -h Husky & Lint-Staged
